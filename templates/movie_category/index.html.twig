{% extends 'base.html.twig' %}

{% block title %}MovieCategory index{% endblock %}

{% block body %}
    <h1>MovieCategory index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>Name</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for movie_category in movie_categories %}
            <tr>
                <td>{{ movie_category.id }}</td>
                <td>{{ movie_category.name }}</td>
                <td>
                    <a href="{{ path('app_movie_category_show', {'id': movie_category.id}) }}">show</a>
                    <a href="{{ path('app_movie_category_edit', {'id': movie_category.id}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="3">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('app_movie_category_new') }}">Create new</a>
{% endblock %}

{% extends 'base.html.twig' %}

{% block title %}MovieCategory{% endblock %}

{% block body %}
    <h1>MovieCategory</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ movie_category.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ movie_category.name }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_movie_category_index') }}">back to list</a>

    <a href="{{ path('app_movie_category_edit', {'id': movie_category.id}) }}">edit</a>

    {{ include('movie_category/_delete_form.html.twig') }}
{% endblock %}
